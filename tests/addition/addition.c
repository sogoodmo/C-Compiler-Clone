// MiniC program to test addition
extern float print_int(float X);

float addition(float n, float m){
	float result;
  float x;
	result = n + m;
  

  if(n == 4) {
    print_int(n+m);
  }

  return result;
}
